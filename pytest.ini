[pytest]
# Explicit as of the 2026-09-04 structure cleanup: pytest only ever
# discovers tests/*.py. Real-provider evaluation harnesses live under
# benchmarks/ (outside this tree entirely) specifically so they can
# never be picked up here, accidentally or otherwise.
testpaths = tests
