# 00 — R&D Master Index

This is the single source of truth for the status of every experiment in
this project. Update it immediately after any experiment produces a result.
See [02_RND_Methodology.md](02_RND_Methodology.md) for how each experiment
is run, measured, and documented.

## Status Legend

- **Not Started** — no code/doc yet
- **In Progress** — being built/run
- **Completed** — result recorded below and in the linked document
- **Blocked** — cannot proceed, reason noted

## Experiments

| ID | Experiment | Status | Result | Document |
|----|------------|--------|--------|----------|
| RND-000 | Project Initialization (Step 0) | Completed | Structure, README, requirements, .gitignore, .env.example, and this doc set created | This index + 01, 02 |
| RND-001 | Screen Capture | Completed | PASS — 6/7 tests passed (SC-001–SC-006), SC-007 SKIPPED (Outlook not running); mss and pyautogui confirmed to agree at true physical 1920x1080 despite 125% Windows scaling | [03_Screen_Capture.md](03_Screen_Capture.md) |
| RND-002 | Outlook Screenshot Dataset | Completed | PASS — 10/15 captured, 1/15 correctly skipped (OUTLOOK-011, no popup available), all human-confirmed before recording and validated PASS by validate_outlook_dataset.py. Annotation-tool DPI-clipping bug fixed; Send bbox corrected; HR-sensitive email caught and rejected (privacy safeguard proven). Optional OUTLOOK-012–015 deliberately not attempted (see doc Limitations). | [04_Outlook_Screenshot_Dataset.md](04_Outlook_Screenshot_Dataset.md) |
| RND-003 | First Vision AI Integration | Completed | PASS — real Gemini gemini-3.6-flash call succeeded after 2 honest failures (gemini-2.5-flash retired HTTP 404; then HTTP 504 timeout at 30s, fixed by raising to 60s). Schema valid, coords in bounds, latency 49064ms, tokens 1452 in / 224 out, cost $0.001929 (verified pricing). | [05_First_Vision_Provider_Integration.md](05_First_Vision_Provider_Integration.md) |
| RND-003B | Multi-Provider Readiness (OpenAI + Anthropic) | Blocked | Abstraction/error-handling proven correct (21 mocked tests), but neither provider has completed a real call across 2 rounds of billing (2 different orgs' API keys) — both authenticate but report zero funded credits. **OpenAI: benchmark pending credits. Anthropic: benchmark pending credits.** Re-run is a one-line command once funded; round 1+2 history preserved in `results/raw/rnd_provider_readiness_{provider}_history.json`, not overwritten. | [05A_Multi_Provider_Readiness.md](05A_Multi_Provider_Readiness.md) |
| RND-004 | Screen Understanding Accuracy | Completed | PASS (Gemini only) — App recognition 10/10=100%, screen-state 8/10=80% (2 misses plausibly explained by RND-002's own ground-truth ambiguity, not model error), relevant-control detection 4/5=80%, correct next-action 3/6=50% (repeated "click Reply" bias found across 3 high-confidence-wrong cases). 2 real evaluator bugs found+fixed and all 10 cases re-scored from original raw data (no re-calls). 4 transient network retries, 0 timeouts, 0 unresolved failures. Cost $0.016005 total (verified pricing). | [06_Vision_Screen_Understanding.md](06_Vision_Screen_Understanding.md) |
| RND-005 | UI Grounding (no clicks) | Completed | **AMENDED by RND-005A** — originally scored 0/4 = 0% (N=4) treating coordinates as native pixels. All 4 predictions were structurally valid, in-bounds, and high-confidence (0.95–1.00) but landed outside every bbox. 0 retries, 0 timeouts. See RND-005A for the corrected reading. | [07_UI_Grounding.md](07_UI_Grounding.md) |
| RND-005A | UI Grounding Coordinate Calibration | Completed | **4/4 = 100% (N=4)** once Gemini's coordinates are correctly interpreted as normalized 0–1000 (Google's documented Gemini spatial-output convention, confirmed via ai.google.dev), not native pixels. All 4 RND-005 predictions land inside their bbox after conversion; verified visually for OUTLOOK-003 (converted point lands centered on the real Reply button). 0 new API calls made — pure local re-analysis of existing RND-005 data. RND-003's identical (478,630) Reply value cross-checked but explicitly not counted as independent evidence (same number, not a second sample). Follow-up RND-005B proposed for direct confirmation. | [07A_Coordinate_Calibration.md](07A_Coordinate_Calibration.md) |
| RND-005B | Coordinate Convention Confirmation | Completed | Fresh, independent live call (OUTLOOK-003/Reply, new prompt, fairness-rule — no hint given) returned raw (477,631), nearly identical to RND-003/RND-005's (478,630) — 3rd independent reproduction. Gemini **declared its convention as "native image pixels" (0.95 confidence) — but that interpretation FAILS**; only the normalized-1000 conversion PASSES (converted to (915.84,681.48), inside the Reply bbox). **Key finding: the model's self-reported coordinate convention is wrong** — never trust it over empirical verification. Coordinate convention now considered sufficiently understood for RND-006 planning; conversion utility validated against 3 independent real calls. RND-006 still requires separate explicit authorization before any clicking. | [07B_Coordinate_Convention_Confirmation.md](07B_Coordinate_Convention_Confirmation.md) |
| RND-006A | Safe Mouse Execution (move-only) | Completed | **3/3 PASS** — email_row (280,327)→(538,353), reply (477,585)→(916,632), reply_editor (700,780)→(1344,842), all human-confirmed. One real foreground-safety abort correctly blocked a move (VS Code stole focus) — recovered only after human confirmed Outlook foreground again. 0 clicks anywhere (structurally unreachable); Send never a valid target. Total cost $0.003755 (4 vision calls, 1 retry). All completion criteria met — RND-006B (Safe Click Execution) proposed, NOT started automatically. | [08_Safe_Mouse_Execution.md](08_Safe_Mouse_Execution.md) |
| RND-006B | Safe Click Execution | Completed | **Attempt 1** (email_row only): PASS (human), but AI verification false negative (verified=false, 0.95 conf., no delay). **Attempt 2** (1.5s stabilization delay + up to 1 retry, never re-clicking) — **3/3 PASS**: email_row `AI_VERIFIED_FIRST_ATTEMPT` (1.0 conf.), reply `ATTEMPT1_TECHNICAL_ERROR_ATTEMPT2_PASSED` (network error on attempt 1, not a model "no" — a second real classification bug found+fixed mid-session so this wasn't misreported as stabilization evidence), reply_editor `AI_VERIFIED_FIRST_ATTEMPT` (1.0 conf., cursor visibly detected). `VISION_VERIFICATION_FALSE_NEGATIVE` never observed under Attempt 2 — coherent evidence the original false negative was a render-timing artifact, not an unreliable model. Both attempts' result files preserved separately, neither overwritten. Total Attempt 2 cost ~$0.008263 (7 vision calls). All completion criteria met — RND-007 proposed, NOT started automatically. | [08B_Safe_Click_Execution.md](08B_Safe_Click_Execution.md) |
| RND-007A | Controlled Text Entry | Completed | **PASS** — fixed sentence "This is a controlled POC test reply." typed into the already-focused reply editor (single `pyautogui.write` call; Enter/Ctrl+Enter/Alt+S/Send structurally absent from the module). All three signals agreed: typing_result=PASS, vision_verification=True (exact text match, confidence 1.0), human_verification=True. Foreground check passed; typing state persisted immediately so a later step's failure could never lose it. Cost $0.001187 (1 verification call, no grounding call needed since editor was already focused from RND-006B). RND-007B (Contextual Reply Generation) proposed, NOT started automatically. | [09_Controlled_Text_Entry.md](09_Controlled_Text_Entry.md) |
| RND-007 (original "Action Verification" concept) | — | Superseded | Post-action state verification (the original Step-0 plan's RND-007) turned out to be needed earlier than planned and was built directly into RND-006B (click) and RND-007A (typing) as each stage's own verification step, rather than as a separate later-numbered experiment. No standalone RND-007 "Action Verification" experiment exists; see RND-006B and RND-007A instead. | [08B_Safe_Click_Execution.md](08B_Safe_Click_Execution.md), [09_Controlled_Text_Entry.md](09_Controlled_Text_Entry.md) |
| RND-007B | Contextual Reply Generation + Draft Entry | Completed | **PASS on Attempt 2 Retry 1** (of 3 attempts, all preserved separately). Attempt 1 FAILED: a single `pyautogui.write()` call with embedded `\n` lost all but the last line of the draft in Outlook's editor (genuine execution bug, not a vision false negative — human-confirmed). Attempt 2 FAILED: same method, plus a second bug found — `cmd_verify()` had no foreground check and silently captured VS Code instead of Outlook; human confirmed nothing was actually typed. Fixed `cmd_verify()` to foreground-check before capture. Attempt 2 Retry 1 PASSED: corrected segment-based typing (one `write()` per line, explicit `press("enter")` between lines, 0.6s focus-settle delay) — exact match, confidence 1.0, human-confirmed, all 6 reply-quality criteria PASS. Total tracked cost $0.005674 (4 tracked vision calls; historical figure preserved as originally recorded, undercount caveat kept). **Pre-RND-008 hardening (post-approval, PASS):** mid-action foreground checks added before every typed segment and every Enter press (aborts immediately on drift, records `abort_stage`/`segment_index_aborted_at`/`segments_typed_before_abort`, no auto-refocus, no auto-retry); `_check_reply_editor_open()`'s previously-untracked Gemini call now accumulates into totals, plus a new `total_latency_ms` accumulator added module-wide. 178/178 tests passing (4 new test functions, 12 behaviors). Send remains fully unimplemented throughout. | [09B_Contextual_Reply_Generation.md](09B_Contextual_Reply_Generation.md) |
| RND-008/009/010 (original "Email Understanding" / "Reply Generation" / "Reply Draft Automation" concepts) | — | Superseded | These three separately-numbered Step-0 concepts were exercised together, earlier than planned, inside RND-007B (email understanding → reply generation → draft typing, gated by human approval at each step) rather than as three standalone later experiments. No standalone RND-008/009/010 experiments under these original titles exist; see RND-007B instead. The "RND-008" number is reused below for **Controlled Send + Send Verification**, matching the live naming used when RND-007B was authorized. | [09B_Contextual_Reply_Generation.md](09B_Contextual_Reply_Generation.md) |
| RND-008 | Controlled Send + Send Verification | Completed | **PASS — SEND_CONFIRMED** on the first live attempt. Real reply sent to the "Mail for project" test email's sender after 4 separately-recorded human approvals (test email, draft, final immediate pre-click, post-send). Send grounded to (851,1030), inside the RND-002 human-annotated ground-truth bbox (800,1022)-(902,1060). Exactly one `pyautogui.click()`, structurally enforced (a second `--execute-send` call raises `SystemExit`). AI post-send verification passed on attempt 1/2 (`AI_VERIFIED_FIRST_ATTEMPT`), human verification agreed — `send_click_result`/`ai_send_verification`/`human_send_verification` kept as 3 separate fields, never collapsed. Total cost $0.006118 (4 vision calls, all real calls including the reply-editor-open state check accumulated per RND-007B's hardening fix). 15 new mocked tests (193 total passing) proving every abort/no-re-send/bbox-reject/max-2-attempts path, none of which was exercised live since this run had no failures. Full end-to-end POC (chained, no per-micro-step gating) proposed next, NOT started automatically. | [10_Controlled_Send_Verification.md](10_Controlled_Send_Verification.md) |
| RND-011 (original "Send Enablement" slot) | — | Superseded | Folded into the reused RND-008 number above (Controlled Send + Send Verification) to match the live naming from RND-007B's conclusion — see that row. | [12_End_to_End_Outlook_POC.md](12_End_to_End_Outlook_POC.md) |
| RND-012 | Full End-to-End POC | Not Started | - | [12_End_to_End_Outlook_POC.md](12_End_to_End_Outlook_POC.md) |
| RND-009A | Windows POC App Interface + Playbook-Ready Architecture | Completed | **UI + architecture preparation only — no Outlook launch, no mouse/keyboard automation, no Send.** Native PySide6 app (`app/`), 4-screen flow (Login → Terms & Permissions → Automation Dashboard → Result), 900x600, native-Windows-DPI-aware. Placeholder login (no real auth/persistence/logging, password masked). Playbook engine (`app/playbook/engine.py`) enforces strictly linear state transitions across all 15 required states; `SENDING` is structurally reachable ONLY from `WAITING_FOR_SEND_APPROVAL` and only after an explicit `approve_send()` call — proven by tests that `READY→SENDING` and `DRAFT_READY→SENDING` are both rejected. Shared `AbortController` (flag-based, no thread-kill) moves the playbook to `ABORTED` and blocks further advancement. `SessionMetrics` skeleton includes `send_click_count` (defaults to 0, for a future max-1-per-session cap). 25 new mocked tests (218 total passing), including a structural proof that no module under `app/` imports or calls pyautogui. 5 real, correctly-rendered screenshots captured via direct widget `grab()` (offscreen Qt platform's font rendering was found broken on this machine — documented, worked around with the native platform instead). RND-009B (Windows Search → Outlook launch → foreground verify) proposed next, NOT started automatically. | [11_RND009A_Windows_App_Playbook_Interface.md](11_RND009A_Windows_App_Playbook_Interface.md) |
| RND-009B | Outlook Launch Playbook | Completed | **PASS on Attempt 2** (of 2, both preserved separately). Start Automation wired to a real `QThread` worker (`OutlookLaunchWorker`) running `OutlookLaunchSteps`: Windows key → type "Outlook" (no Enter) → screenshot → Gemini grounding (dedicated prompt, target-only, never "what next") → 0-1000 coordinate conversion → validation gates (search visible, label contains "outlook", confidence ≥0.6, in-bounds) → single click → bounded polling (2.0s + 1.0s ticks, 18s timeout) → Vision launch verification → `LAUNCHING_OUTLOOK → OUTLOOK_VERIFIED`. **Attempt 1 FAILED** (`SEARCH_STATE_LOST_BEFORE_CLICK`): the original mid-run "show grounding result, ask approval" design let this VS Code window regain foreground between grounding and click, correctly blocking the click but invalidating the run — this is exactly why the design changed. **Redesigned to bounded session approval**: one upfront yes/no covers the whole uninterrupted sequence; code-level validation gates (not a human answering mid-flight) are the real-time safety mechanism. **Attempt 2 PASSED**: label "Outlook", raw (130,303)→converted (250,327), confidence 0.98, click executed, Outlook foreground detected in 2001.9ms, Vision confirmed (splash screen, confidence 1.0, independently screenshot-verified). `send_click_count` stayed 0 throughout both attempts; new dedicated Windows-Search-state allowlist (never reusing "Outlook must be foreground") confirmed correct live (`get_foreground_window_title()` reported literal `'Search'`). Total cost across both attempts ~$0.0041 (3 vision calls). Scope strictly stops at `OUTLOOK_VERIFIED` — no email interaction, no Reply, no Send anywhere in this module. **Readiness hardening (post-approval adjustment, same stage):** a real gap found in Attempt 2's own result — `outlook_visible: True` was accepted as PASS even though the actual screenshot showed Outlook's splash/loading screen, not a usable UI. Fixed by splitting into two explicit signals (`splash_screen_visible`, `ready_for_interaction`) that a splash screen can never satisfy; `verify_outlook_with_vision()` replaced by `verify_outlook_readiness()`, a bounded 2-attempt retry loop (6.0s initial wait, 3.0s retry wait), each attempt recorded separately; exhausting both attempts still loading now classifies `OUTLOOK_READY_TIMEOUT` rather than passing. A second gap (verification screenshot filename never persisted) fixed in the same pass. Original Attempt 2 PASS result preserved unchanged, not retroactively reclassified. 251/251 tests passing (33 new/updated total for RND-009B). **Readiness live validation (closes the prior open item):** re-ran the launch, readiness attempt 1 hit a transient Gemini HTTP 504 (technical error, not "still loading"), retried via a new standalone `--check-readiness` command against the same already-launched Outlook window — attempt 2 PASSED with `ready_for_interaction: true`, `splash_screen_visible: false`, confidence 1.0, independently confirmed against a screenshot showing a genuine, fully-rendered inbox (folder list, messages, reading pane) — concretely proving the hardening distinguishes exactly the kind of splash screen Attempt 2 originally (incorrectly) passed. Preserved as its own labeled result (`_readiness_validation`), with the original canonical/`_attempt1` files also explicitly copied to `_attempt2` first — none overwritten. One minor gap found and documented (not silently worked around): `--check-readiness` always starts its own attempt numbering at 1 rather than resuming an in-progress run's count. RND-009C (`OUTLOOK_VERIFIED → FINDING_EMAIL`) proposed next, NOT started automatically. | [12_RND009B_Outlook_Launch_Playbook.md](12_RND009B_Outlook_Launch_Playbook.md) |
| RND-009C | Find + Open Email Playbook | Completed | **PASS on the first live attempt.** Chained playbook: `READY → LAUNCHING_OUTLOOK → OUTLOOK_VERIFIED → FINDING_EMAIL → EMAIL_OPENED → STOP`. `FindOpenEmailSteps` composes RND-009B's `OutlookLaunchSteps` (reused, not duplicated) for the launch+readiness phases, then adds two new phases: find target email (playbook-hardcoded `subject="Mail for project"`, `sender="Yash"` — Vision never chooses the target) and verify the correct email opened (not just "an email is open"). Same bounded-upfront-approval design as RND-009B's redesign — one yes covers the whole chain, no mid-run pause. Live: Outlook launched (2,005.5ms), readiness PASS attempt 1/1, email grounded correctly (target_visible/matched_subject/matched_sender all correct), single click, email-open verification PASS attempt 1/2 (subject_match, sender_match, body_visible all true) — independently confirmed via screenshot. 4 vision calls, $0.006122, ~74.2s total elapsed (derived by hand — a real gap found: `session_start`/`session_end`/`total_elapsed_ms` fields exist but nothing populates them, flagged for RND-009D). 7 new failure classifications added (`TARGET_EMAIL_NOT_VISIBLE`, `TARGET_EMAIL_MISMATCH`, `EMAIL_GROUNDING_INVALID`, `EMAIL_GROUNDING_OUT_OF_BOUNDS`, `OUTLOOK_FOREGROUND_LOST`, `EMAIL_OPEN_VERIFICATION_FAILED`, `WRONG_EMAIL_OPENED`). Target-not-visible deliberately does not scroll/search (out of scope this run). No Reply/typing/Send code path anywhere in this module (structurally proven); `send_click_count` stayed 0. **Pre-RND-009D hardening (post-approval, same stage):** the `total_elapsed_ms` gap (RND-009C needed ~74.2s manually derived) fixed — `FindOpenEmailSteps.start_session()`/`finalize_session()` now populate `session_start`/`session_end`/`total_elapsed_ms` automatically on every terminal outcome (PASS/FAIL/ABORTED/ERROR), kept strictly separate from `total_latency_ms` (Vision-call-only). Persisted in raw results, the summary report's new "Timing" section, `SessionMetrics` (new field, wired via a previously-unconnected `metrics_update` signal — a second small gap closed in the same pass), and `ResultPage`. Historical RND-009C result left unmodified (`total_elapsed_ms: null`, ~74.2s figure stays labeled as manually derived). 279/279 tests passing (28 new total for RND-009C: 22 original + 6 for this hardening). RND-009D (Reply + Draft) proposed next, NOT started automatically. | [13_RND009C_Find_Open_Email_Playbook.md](13_RND009C_Find_Open_Email_Playbook.md) |
| RND-013 | Model Benchmark (2nd provider) | Not Started | - | [13_Model_Benchmark.md](13_Model_Benchmark.md) |
| RND-014 | Final R&D Findings | Not Started | - | [15_Final_RND_Findings.md](15_Final_RND_Findings.md) |

## Notes

- `docs/14_Known_Limitations.md` is updated continuously as limitations are
  discovered across any experiment, not tied to a single RND ID.
- No row above moves to "Completed" without an actual test run behind it.
- Document numbering has been revised twice, each time an RND stage turned
  out to need its own dedicated document instead of sharing one with the
  next stage (see [02_RND_Methodology.md](02_RND_Methodology.md) §Document
  Numbering History for both revisions): first when RND-002 (Outlook
  dataset) was split out, then when RND-003 (first provider integration)
  was split from RND-004 (screen understanding accuracy). `05` is now the
  provider-integration doc; everything from `06` onward shifted by one
  again as a result.
