"""Real-provider benchmark/evaluation tooling — makes actual Claude/Gemini
API calls, never run by normal `pytest` (see benchmarks/claude/ and
benchmarks/gemini/ for the two provider-specific harnesses, and
benchmarks/results/ for their JSON/Markdown output)."""
